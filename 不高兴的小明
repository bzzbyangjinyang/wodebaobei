#include <stdio.h>
int main()
{
	int n, a, b,sum, i, j;
	scanf("%d", &n);
	while(n--)
	{
		sum = 0;
		j = 0;


		for(i = 1; i <= 7; i++)
		{
		scanf("%d %d", &a, &b);
			if(a+b>8 && a+b>sum)
			{
				j = i;
				sum = a + b;
			}
		}
		printf("%d\n", j);
	}
	return 0;
}
