#include <stdio.h>
#include <string.h> 
int main()
{
	int n;
	scanf("%d",&n);
	char a[n][3];
	int i,j;
	for(i=0;i<n;i++)
	{
		for(j=0;j<3;j++)
		{
			scanf("%c",a[i][j]);
		}
	}
	char t;
	for(i=0;i<n;i++)
	{
		if(a[i][0]<a[i][1])
		{
			if(a[i][1]<a[i][2])
			{
				printf("%c %c %c",a[i][0],a[i][1],a[i][2]); 
			}
			else
			{
				printf("%c %c %c",a[i][0],a[i][2],a[i][1]);
			}
		}
		else
		{
			if(a[i][0]<a[i][2])
			{
				printf("%c %c %c",a[i][1],a[i][0],a[i][2]);
			}
			else
			{
				printf("%c %c %c",a[i][1],a[i][2],a[i][0]);
			}
		}
	}
	return 0;
}
